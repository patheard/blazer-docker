{
    // Increase Puma worker timeout before debugging:
    // export PUMA_TIMEOUT=3600
    "version": "0.2.0",
    "configurations": [
        {
            "type": "rdbg",
            "name": "Ruby debug",
            "request": "attach"
        }
    ]
}